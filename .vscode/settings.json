// Place your settings in this file to overwrite default and user settings.
{
        "files.exclude": {
        "**/.git": false,
        "**/*.js": { "when": "$(basename).ts" } // hides the .js version of .ts file
    }
}